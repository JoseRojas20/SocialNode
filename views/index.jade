extends layout

block content
  h1
    Social Node
  if(user)
      ul
        li
          img(src="#{user.photo}")
        li Bienvenido #{user.name}
        li
          a(href='logout') Salir
  else
      ul
        li
          a(hrefa='auth/twitter') Login con Twitter
        li
          a(href='auth/facebook') Login con Facebook
